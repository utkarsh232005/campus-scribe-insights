{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "dist",
          "installCommand": "npm install --no-optional && npm install @swc/core-linux-x64-gnu"
        }
      }
    ]
  }